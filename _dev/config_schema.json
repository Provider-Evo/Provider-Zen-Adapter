{
  "type": "object",
  "properties": {
    "api_keys": {
      "type": "array",
      "title": "API Keys",
      "items": {
        "type": "string"
      },
      "default": [],
      "description": "Zen 平台 API 密钥列表"
    },
    "use_proxy_pool": {
      "type": "boolean",
      "title": "使用代理池",
      "default": false,
      "description": "启用 opencode 代理池（需配合 USE_PROXY_POOL=True）"
    }
  }
}
